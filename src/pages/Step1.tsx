import { Button, Typography } from "@mui/material";
import { PageLayout } from "../layouts";

export function Step1() {
  return (
    <PageLayout>
      <Typography variant="h4" gutterBottom>
        Hello MUI + Vite + TypeScript
      </Typography>
    </PageLayout>
  );
}
